(function () {
  const main = async function () {
    // Pure userstyle; nothing to do
  };

  const clean = async function () {
    // Pure userstyle; nothing to undo
  };

  return { main, clean, stylesheet: true };
})();
